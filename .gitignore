backend/.venv
backend/.env
__pycache__/
.vscode/
viewer.html
vercel.json
boot.sh

# Windows Zone.Identifier alternate data streams (WSL artefact)
*:Zone.Identifier*

# Runtime state (deduplication signatures, etc) — created at startup
backend/generated/verification_failures.jsonl

# Supabase CLI local artifacts
supabase/.temp/
supabase/.branches/